<p id="notice"><%= notice %></p>

<h1>Portfolios</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Sub title</th>
      <th>Description</th>
      <th>Thumb img</th>
      <th>Main img</th>
      <th>Gh link</th>
      <th>Url link</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @portfolios.each do |portfolio| %>
      <tr>
        <td><%= portfolio.title %></td>
        <td><%= portfolio.sub_title %></td>
        <td><%= portfolio.description %></td>
        <td><%= portfolio.thumb_img %></td>
        <td><%= portfolio.main_img %></td>
        <td><%= portfolio.gh_link %></td>
        <td><%= portfolio.url_link %></td>
        <td><%= link_to 'Show', portfolio %></td>
        <td><%= link_to 'Edit', edit_portfolio_path(portfolio) %></td>
        <td><%= link_to 'Destroy', portfolio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Portfolio', new_portfolio_path %>
